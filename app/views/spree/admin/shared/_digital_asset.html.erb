<div class="text-center asset-area">
  <div class="digital-assets-image">
    <%= image_tag(digital_asset.attachment.url(:small), class: 'img-thumbnail attachment' ,
      data: { id: digital_asset.id, name: digital_asset.name } )  %>
  </div>
  <p>
    <%= digital_asset.name %>
  </p>
  <div class="btn-group corner-menu">
    <button type="button" class="btn btn-xs btn-link dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
   <span class="glyphicon glyphicon-option-vertical"></span>
    </button>
    <ul class="dropdown-menu">
      <li>
        <%= link_to 'Remove', admin_digital_asset_path(digital_asset), method: :delete, remote: true, class: 'delete_digital_asset_button' %>
      </li>
      <li>
        <%= link_to 'Edit', edit_admin_digital_asset_path(digital_asset) %>
      </li>
      <li role="separator" class="divider"></li>
      <li><%= link_to 'Download', digital_asset.attachment.url(:original, false), target: '_blank' %></li>
    </ul>
  </div>
</div>
